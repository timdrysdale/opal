include "BaseTypes.fbs";

namespace Veneris.Communications;

attribute "priority"; // Necesario al pasar el binario en "runtime"

table Header{
	type: VenerisMessageTypes; 
		// Tipos
		//
		// 0: Reservado (futuro uso, gestion general)
		// 1: Create
		// 2: Destroy
		// 3: CAM (Mov, update POSITION)
		// 4: ExternalTime
		// 5: End
		//
	size:uint;  
}

root_type Header;
